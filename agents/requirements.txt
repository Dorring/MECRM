# AI Agent Layer - Enterprise CRM
# Requirements for LangGraph-based agents

# Core
langgraph>=0.0.50
langchain>=1.3.13
langchain-community>=0.4.2

# LLM providers (Ollama local fallback and OpenAI-compatible NVIDIA NIM)
langchain-ollama>=0.0.1
langchain-openai>=1.1.0

# Async
asyncio
aiohttp>=3.14.1

# Kafka
aiokafka>=0.14.0

# Database
asyncpg>=0.29.0
sqlalchemy>=2.0.51

# Vector Store
weaviate-client>=4.4.0

# HTTP
httpx>=0.26.0
fastapi>=0.139.0
uvicorn[standard]>=0.27.0

# Redis
redis>=8.0.1

# Metrics
prometheus-client>=0.20.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
structlog>=23.2.0
uuid6>=2023.5.2

# i18n language detection (P1-12). langdetect is the primary detector used by
# intelligence/i18n/language_detector.py. fasttext is an optional, heavier
# fallback (model file required); install separately if FASTTEXT_MODEL_PATH is
# set. langdetect is required for the i18n unit tests to pass.
langdetect>=1.0.9
# fasttext is intentionally NOT a hard dependency: the detector falls back to
# langdetect when the fasttext model is absent. Install fasttext>=0.9.2 only
# if you enable use_fasttext=True.

# OpenTelemetry
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.44.0
opentelemetry-instrumentation>=0.64b0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=7.1.0
